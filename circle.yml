deployment:
  development:
    branch: development
    commands:
      - npm run build
      - aws s3 sync sync mydir/ s3://mytestmystique
  production:
    branch: master
    commands:
      - npm run build
      - aws s3 sync sync mydir/ s3://mytestmystique
