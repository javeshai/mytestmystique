machine:
  node:
    version:  4.2.0

dependencies:
  override:
    - npm install
    - bundle install
    - sudo pip install awscli
   

deployment:
 aws:
  branch: master
  commands:
- aws s3 sync mydir/ s3://mytestmystique

